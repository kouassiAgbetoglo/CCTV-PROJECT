import settings from '../../Assests/settings.svg';

const UserSettings = () => {
    return (
        <>
            <img src={settings} alt='User Settings' style={{ width: '60px', height: '60px' }}/>
        </>
    );
}

export default UserSettings;
